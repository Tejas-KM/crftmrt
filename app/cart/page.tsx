import { CartClient } from "@/components/CartClient"

export const metadata = {
  title: "Shopping Cart - CraftMart",
  description: "Review your selected items and proceed to checkout",
}

export default function CartPage() {
  return <CartClient />
}
